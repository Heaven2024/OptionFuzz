(set-option :sat.phase basic_caching)
(set-option :sat.phase.sticky true)
(set-option :sat.restart.fast true)
(set-logic QF_AUFLIA)
(declare-fun a () (Array Int Int))
(declare-fun i () Int)
(assert (> (select a i) 0))
(assert (< i 10))
(check-sat)